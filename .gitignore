# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — все .env* игнорируются (создаются install.sh со абсолютным путём к БД,
# который у каждого пользователя свой; для боевого MS SQL — секреты в .env.local)
.env
.env.local
.env.production
.env.development.local

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
# ВНИМАНИЕ: НЕ игнорировать 'test' глобально — это блокирует /api/settings/sql/test/
# Если нужно игнорировать конкретную папку test — указывайте путь: /path/to/test/
prompt

server.log
# Skills directory
/skills/

# RK7 Analytics — generated/runtime artifacts
/db/
/upload/
/download/
/.zscripts/
/scripts/test_prisma*.ts
*.db
*.db-journal.env
